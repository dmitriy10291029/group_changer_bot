#!/bin/bash
# Скрипт для первоначальной настройки на сервере
# Выполните этот скрипт один раз на сервере

echo "🔧 Настройка сервера для Group Changer Bot..."

# Клонируем репозиторий
if [ ! -d "group_changer_bot" ]; then
    echo "📥 Клонируем репозиторий..."
    git clone git@github.com:dmitriy10291029/group_changer_bot.git
    cd group_changer_bot
else
    echo "📁 Репозиторий уже существует, переходим в директорию..."
    cd group_changer_bot
fi

# Создаём виртуальное окружение
if [ ! -d "venv" ]; then
    echo "🐍 Создаём виртуальное окружение..."
    python3 -m venv venv
else
    echo "✅ Виртуальное окружение уже существует"
fi

# Активируем виртуальное окружение и устанавливаем зависимости
echo "📦 Устанавливаем зависимости..."
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt

echo ""
echo "✅ Настройка завершена!"
echo ""
echo "Для запуска бота используйте:"
echo "  source venv/bin/activate"
echo "  python bot.py"
echo ""
echo "Или через screen (для фонового запуска):"
echo "  screen -S bot"
echo "  source venv/bin/activate"
echo "  python bot.py"
echo "  # Нажмите Ctrl+A, затем D для отсоединения"

