# 🔄 Group Changer Bot — Техническая документация

## 📋 Описание проекта

Телеграм-бот для студентов программы ИАД, который помогает найти людей для взаимного обмена группами. Бот автоматически находит "мэтчи" — когда два человека хотят перевестись в группы друг друга.

**Целевая аудитория:** 10-100 студентов ИАД  
**Группы:** ИАД-1 — ИАД-10

---

## ✨ Основные фичи

### 1. 📝 Регистрация и настройка профиля
- Указание текущей группы (ИАД 1-10)
- Выбор желаемых групп для перевода (можно несколько)
- Подтверждение данных перед сохранением

### 2. 🎯 Автоматический поиск мэтчей
- При каждом изменении данных или новой регистрации — проверка на мэтчи
- Мэтч = взаимный интерес (A хочет в группу B, B хочет в группу A)
- Автоматическая рассылка уведомлений обоим участникам мэтча

### 3. 🔍 Ручная проверка мэтчей
- Кнопка для проверки актуальных мэтчей в любой момент
- Список контактов людей, с которыми можно обменяться

### 4. ✏️ Редактирование данных
- Смена текущей группы (если уже перевёлся)
- Изменение списка желаемых групп
- При изменении — автоматическая перепроверка мэтчей

### 5. 🚪 Выход из системы
- Кнопка "Больше не ищу" — удаление из базы данных
- Автоматическое удаление при блокировке бота (если возможно технически)

---

## 🎨 Интерфейс бота (UX Flow)

### Стартовый экран (`/start`)

```
👋 Привет! Я помогу тебе найти человека для обмена группами ИАД.

🔄 Как это работает:
1. Укажи свою текущую группу
2. Выбери группы, в которые хочешь перевестись  
3. Я найду людей, которые хотят к тебе — и скину контакты!

📌 Начнём?
```

**Кнопки:**
- `🚀 Начать регистрацию`

---

### Выбор текущей группы

```
📍 В какой группе ты сейчас учишься?
```

**Инлайн-кнопки (2 ряда по 5):**
```
[ ИАД-1 ] [ ИАД-2 ] [ ИАД-3 ] [ ИАД-4 ] [ ИАД-5 ]
[ ИАД-6 ] [ ИАД-7 ] [ ИАД-8 ] [ ИАД-9 ] [ ИАД-10 ]
```

---

### Выбор желаемых групп

```
🎯 В какие группы хочешь перевестись?

Выбери одну или несколько групп, потом нажми «Готово»

✅ — выбрано | ⬜ — не выбрано
```

**Инлайн-кнопки (с toggle-состоянием):**
```
[ ⬜ ИАД-1 ] [ ⬜ ИАД-2 ] [ ⬜ ИАД-3 ] [ ⬜ ИАД-4 ] [ ⬜ ИАД-5 ]
[ ⬜ ИАД-6 ] [ ⬜ ИАД-7 ] [ ⬜ ИАД-8 ] [ ⬜ ИАД-9 ] [ ⬜ ИАД-10 ]

                    [ ✅ Готово ]
```

*Текущая группа пользователя не показывается в списке*

---

### Подтверждение

```
📋 Проверь данные:

👤 Твоя группа: ИАД-3
🎯 Хочешь перевестись в: ИАД-1, ИАД-7

Всё верно?
```

**Кнопки:**
- `✅ Подтвердить`
- `✏️ Изменить`

---

### Главное меню (после регистрации)

```
🏠 Главное меню

👤 Твоя группа: ИАД-3
🎯 Ищешь: ИАД-1, ИАД-7

Что хочешь сделать?
```

**Кнопки (Reply Keyboard):**
```
[ 🔍 Проверить мэтчи ]
[ ✏️ Изменить мою группу ] [ 🎯 Изменить желаемые ]
[ 🚪 Больше не ищу ]
```

---

### Уведомление о мэтче

```
🎉 Есть мэтч!

Нашёлся человек для обмена:

👤 @username
📍 Сейчас в: ИАД-7  
🎯 Хочет в: ИАД-3 (твою группу!)

💬 Напиши ему и договоритесь об обмене!
```

---

### Список мэтчей (по кнопке "Проверить мэтчи")

**Если есть мэтчи:**
```
🔍 Твои актуальные мэтчи:

1️⃣ @user1 — ИАД-7 → хочет в ИАД-3
2️⃣ @user2 — ИАД-1 → хочет в ИАД-3

💬 Свяжись с ними!
```

**Если мэтчей нет:**
```
😔 Пока мэтчей нет

Но не переживай! Как только кто-то захочет в твою группу — я сразу напишу.

💡 Совет: чем больше групп выберешь — тем выше шанс найти мэтч!
```

---

### Выход из системы

```
🚪 Ты уверен, что хочешь удалить свои данные?

Тебя больше не будут находить для обмена, и ты не будешь получать уведомления.
```

**Кнопки:**
- `✅ Да, удалить`
- `❌ Отмена`

**После удаления:**
```
👋 Данные удалены!

Если передумаешь — просто напиши /start
```

---

## 🗄️ Структура базы данных

### Таблица `users`

| Поле | Тип | Описание |
|------|-----|----------|
| `telegram_id` | INTEGER PRIMARY KEY | ID пользователя в Telegram |
| `username` | TEXT | @username (может быть NULL) |
| `first_name` | TEXT | Имя пользователя |
| `current_group` | INTEGER | Текущая группа (1-10) |
| `created_at` | TIMESTAMP | Дата регистрации |
| `updated_at` | TIMESTAMP | Дата последнего обновления |

### Таблица `desired_groups`

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INTEGER PRIMARY KEY | ID записи |
| `telegram_id` | INTEGER FK | ID пользователя |
| `desired_group` | INTEGER | Желаемая группа (1-10) |

---

## ⚙️ Технические требования

### Стек технологий
- **Язык:** Python 3.11+
- **Фреймворк:** aiogram 3.x (асинхронный)
- **База данных:** SQLite (достаточно для 100 пользователей)
- **Деплой:** Ubuntu сервер

### Структура проекта
```
group_changer_bot/
├── bot.py              # Точка входа
├── config.py           # Конфигурация (токен и т.д.)
├── database.py         # Работа с БД
├── handlers/
│   ├── __init__.py
│   ├── start.py        # /start и регистрация
│   ├── profile.py      # Изменение данных
│   └── matches.py      # Поиск мэтчей
├── keyboards/
│   ├── __init__.py
│   └── keyboards.py    # Все клавиатуры
├── utils/
│   ├── __init__.py
│   └── matcher.py      # Логика поиска мэтчей
├── requirements.txt
└── README.md
```

---

## 🔄 Алгоритм поиска мэтчей

```python
def find_matches(user_id):
    """
    Мэтч = когда:
    1. User A в группе X, хочет в группу Y
    2. User B в группе Y, хочет в группу X
    """
    user = get_user(user_id)
    matches = []
    
    for desired_group in user.desired_groups:
        # Ищем людей из желаемой группы, которые хотят в нашу
        potential_matches = get_users_from_group(desired_group)
        for candidate in potential_matches:
            if user.current_group in candidate.desired_groups:
                matches.append(candidate)
    
    return matches
```

---

## 📲 Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Начало работы / регистрация |
| `/menu` | Главное меню |
| `/matches` | Проверить мэтчи |
| `/edit` | Изменить данные |
| `/delete` | Удалить аккаунт |
| `/help` | Справка |

---

## 🚀 Деплой

```bash
# На сервере
ssh admin@your-server

# Клонируем репо
git clone git@github.com:dmitriy10291029/group_changer_bot.git
cd group_changer_bot

# Создаём виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt

# Запускаем через systemd или screen
python bot.py
```

---

## 📝 TODO при разработке

- [ ] Базовая структура проекта
- [ ] Подключение к БД (SQLite)
- [ ] Хендлер /start с регистрацией
- [ ] Клавиатуры выбора групп
- [ ] Сохранение в БД
- [ ] Логика поиска мэтчей
- [ ] Уведомления о мэтчах
- [ ] Редактирование профиля
- [ ] Удаление из базы
- [ ] Деплой на сервер

