# 🚀 Инструкция по деплою бота

## ✅ Что уже сделано

1. ✅ Весь код запушен в git репозиторий
2. ✅ Все файлы готовы к деплою

## 📋 Шаги для деплоя

### 1. Подключитесь к серверу

```bash
ssh admin@45.155.204.132
```

### 2. Первая настройка (если ещё не делали)

Если проект ещё не клонирован на сервер:

```bash
# Клонируем репозиторий
git clone git@github.com:dmitriy10291029/group_changer_bot.git
cd group_changer_bot

# Создаём виртуальное окружение
python3 -m venv venv

# Активируем и устанавливаем зависимости
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

### 3. Обновление кода (если проект уже есть)

```bash
cd group_changer_bot
git pull origin main

# Обновляем зависимости (если нужно)
source venv/bin/activate
pip install -r requirements.txt
```

### 4. Запуск бота

#### Вариант 1: Через screen (рекомендуется)

```bash
# Создаём новую сессию screen
screen -S bot

# Активируем виртуальное окружение
cd ~/group_changer_bot
source venv/bin/activate

# Запускаем бота
python bot.py

# Отсоединяемся от screen: нажмите Ctrl+A, затем D
```

**Для повторного подключения к сессии:**
```bash
screen -r bot
```

**Для остановки бота:**
```bash
screen -r bot
# Нажмите Ctrl+C для остановки
```

#### Вариант 2: Через systemd (для автозапуска)

Создайте файл `/etc/systemd/system/group-changer-bot.service`:

```bash
sudo nano /etc/systemd/system/group-changer-bot.service
```

Вставьте следующее содержимое:

```ini
[Unit]
Description=Group Changer Bot
After=network.target

[Service]
Type=simple
User=admin
WorkingDirectory=/home/admin/group_changer_bot
Environment="PATH=/home/admin/group_changer_bot/venv/bin"
ExecStart=/home/admin/group_changer_bot/venv/bin/python /home/admin/group_changer_bot/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:

```bash
# Перезагружаем systemd
sudo systemctl daemon-reload

# Включаем автозапуск
sudo systemctl enable group-changer-bot

# Запускаем бота
sudo systemctl start group-changer-bot

# Проверяем статус
sudo systemctl status group-changer-bot

# Просмотр логов
sudo journalctl -u group-changer-bot -f
```

### 5. Проверка работы

После запуска бота проверьте:

1. **Логи бота** — должны быть сообщения о запуске
2. **Telegram бот** — отправьте `/start` боту и проверьте ответ

## 🔧 Полезные команды

### Проверка запущенных процессов

```bash
ps aux | grep bot.py
```

### Просмотр логов (если используете screen)

```bash
screen -r bot
```

### Остановка бота

```bash
# Если через screen
screen -r bot
# Затем Ctrl+C

# Если через systemd
sudo systemctl stop group-changer-bot
```

### Перезапуск бота

```bash
# Если через systemd
sudo systemctl restart group-changer-bot

# Если через screen
screen -r bot
# Ctrl+C для остановки
python bot.py  # для запуска
```

## 📝 Обновление бота в будущем

Когда нужно обновить код:

```bash
cd ~/group_changer_bot
git pull origin main
source venv/bin/activate
pip install -r requirements.txt

# Перезапустите бота (см. команды выше)
```

## ⚠️ Возможные проблемы

### Бот не запускается

1. Проверьте, что виртуальное окружение активировано
2. Проверьте, что все зависимости установлены: `pip install -r requirements.txt`
3. Проверьте логи на наличие ошибок

### Бот не отвечает

1. Проверьте, что бот запущен: `ps aux | grep bot.py`
2. Проверьте токен в `config.py`
3. Проверьте интернет-соединение на сервере

### Ошибки подключения к базе данных

База данных создаётся автоматически при первом запуске. Убедитесь, что у пользователя есть права на запись в директорию проекта.

## 📞 Контакты

**Бот:** t.me/YetAnotherBotForGroupChangingBotы  
**Сервер:** admin@45.155.204.132

