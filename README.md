# 🔄 Group Changer Bot

Телеграм-бот для студентов программы ИАД, который помогает найти людей для взаимного обмена группами.

## 📋 Описание

Бот автоматически находит "мэтчи" — когда два человека хотят перевестись в группы друг друга, и отправляет им уведомления с контактами.

**Группы:** ИАД-1 — ИАД-10  
**Целевая аудитория:** 10-100 студентов

## 🚀 Быстрый старт

### Локальная разработка

1. Клонируй репозиторий:
```bash
git clone git@github.com:dmitriy10291029/group_changer_bot.git
cd group_changer_bot
```

2. Создай виртуальное окружение:
```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. Установи зависимости:
```bash
pip install -r requirements.txt
```

4. Запусти бота:
```bash
python bot.py
```

## 🚀 Деплой на сервер

### Подключение к серверу

```bash
ssh admin@45.155.204.132
```

### Установка на сервере

```bash
# Клонируем репо
git clone git@github.com:dmitriy10291029/group_changer_bot.git
cd group_changer_bot

# Создаём виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt
```

### Запуск через screen (для фоновой работы)

```bash
screen -S bot
python bot.py
# Нажми Ctrl+A, затем D для отсоединения от screen
```

### Запуск через systemd (рекомендуется)

Создай файл `/etc/systemd/system/group-changer-bot.service`:

```ini
[Unit]
Description=Group Changer Bot
After=network.target

[Service]
Type=simple
User=admin
WorkingDirectory=/home/admin/group_changer_bot
Environment="PATH=/home/admin/group_changer_bot/venv/bin"
ExecStart=/home/admin/group_changer_bot/venv/bin/python bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl enable group-changer-bot
sudo systemctl start group-changer-bot
sudo systemctl status group-changer-bot
```

## 📁 Структура проекта

```
group_changer_bot/
├── bot.py              # Точка входа
├── config.py           # Конфигурация (токен бота)
├── database.py         # Работа с БД (SQLite)
├── handlers/
│   ├── start.py        # /start и регистрация
│   ├── profile.py      # Изменение данных профиля
│   ├── matches.py      # Проверка мэтчей
│   └── help.py         # Команда /help
├── keyboards/
│   └── keyboards.py    # Все клавиатуры бота
├── utils/
│   └── matcher.py      # Логика поиска мэтчей
├── requirements.txt    # Зависимости Python
└── README.md
```

## 📲 Команды бота

- `/start` - Начало работы / регистрация
- `/menu` - Главное меню
- `/matches` - Проверить мэтчи
- `/help` - Справка

## ✨ Основные фичи

- ✅ Регистрация с выбором текущей и желаемых групп
- ✅ Автоматический поиск мэтчей при регистрации и изменении данных
- ✅ Уведомления о новых мэтчах
- ✅ Редактирование текущей группы и желаемых групп
- ✅ Ручная проверка мэтчей по кнопке
- ✅ Удаление из базы через кнопку "Больше не ищу"
- ✅ Автоматическое удаление при блокировке бота

## 🗄️ База данных

Используется SQLite (`bot_database.db`). База создаётся автоматически при первом запуске.

**Таблицы:**
- `users` - данные пользователей
- `desired_groups` - желаемые группы пользователей

## 📝 Техническая документация

Подробная документация с описанием интерфейса и алгоритмов находится в файле [TECH_DOC.md](TECH_DOC.md).

## 🔧 Технологии

- Python 3.11+
- aiogram 3.x (асинхронный фреймворк для Telegram Bot API)
- SQLite (база данных)
- Ubuntu (сервер)

## 📞 Контакты бота

**Токен:** указан в `config.py`  
**Ссылка:** t.me/YetAnotherBotForGroupChangingBotы
